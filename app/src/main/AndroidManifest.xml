<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cookandroid.exam">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Exam">
        <activity
            android:name=".Activity.SetCharacterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.BottomNaviActivity" />
        <activity
            android:name=".Activity.ScheduleCheckAcitivity"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity
            android:name=".Activity.ScheduleUpdateActivity"
            android:theme="@style/DialogTheme.Exam" />
        <activity
            android:name=".Activity.AddRoutineActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".Activity.EditMypageActivity"
            android:theme="@android:style/Theme.Dialog" />
    </application>

</manifest>